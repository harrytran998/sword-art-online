tasks:
  lint:
    command: "oxlint --type-aware"
    inputs:
      - "src/**/*.ts"
      - "src/**/*.tsx"
      - "tsconfig.json"

  format:
    command: "oxfmt ."
    inputs:
      - "src/**/*.ts"
      - "src/**/*.tsx"

  typecheck:
    command: "bun tsc --noEmit"
    inputs:
      - "src/**/*.ts"
      - "src/**/*.tsx"
      - "tsconfig.json"
